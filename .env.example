# KB Platform Gateway - Environment Configuration
# Copy this file to .env and fill in your values
#
# Priority: System environment variables > .env file > defaults

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
GIN_MODE=debug

# Python LlamaIndex Core Service
PYTHON_CORE_HOST=python-llama-core
PYTHON_CORE_PORT=8000

# PostgreSQL Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=kb_user
DB_PASSWORD=kb_password
DB_NAME=kb_platform
DB_SSLMODE=disable

# AWS S3 (or S3-compatible service)
S3_BUCKET=kb-documents
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
# Optional: For S3-compatible services (MinIO, LocalStack, etc.)
# S3_ENDPOINT=https://s3.amazonaws.com

# Temporal Workflow Engine
TEMPORAL_HOST=temporal
TEMPORAL_PORT=7233
TEMPORAL_NAMESPACE=default

# Notes:
# - Values in .env override defaults in code
# - System environment variables override .env file
# - Do not commit .env file with real credentials
# - Add .env to .gitignore
